# --- Python ---
__pycache__/
*.py[cod]
*.pyo
*.egg-info/
.pytest_cache/
.mypy_cache/
.ruff_cache/

# --- Build / packaging artifacts (regenerated by `pip install -e .` / PyInstaller) ---
build/
dist/
*.spec.bak

# --- Local, machine-specific runtime state (generated by the app, not source) ---
settings.json
sessions/
output/
tmp_*
tmp_*/

# --- Local development-tool session state (not part of the OCRing project) ---
.claude/

# --- Editor / OS local files ---
.vscode/
.idea/
Thumbs.db
.DS_Store

# --- Local databases and logs (never intentional fixtures) ---
*.db
*.db-journal
*.log

# --- Internal development-history records (kept locally, not published) ---
PROJECT_MAP.md
/spec/
/spec-receipt/
